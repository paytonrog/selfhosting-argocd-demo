apiVersion: v1
kind: Service
metadata:
  name: temporal-app-worker
  labels:
    app.kubernetes.io/name: temporal-app-worker
spec:
  selector:
    app.kubernetes.io/name: temporal-app-worker
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
