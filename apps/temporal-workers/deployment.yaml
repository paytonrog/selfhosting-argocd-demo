apiVersion: apps/v1
kind: Deployment
metadata:
  name: temporal-worker
  labels:
    app.kubernetes.io/name: temporal-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: temporal-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: temporal-worker
    spec:
      serviceAccountName: temporal-worker
      containers:
        - name: worker
          image: ghcr.io/temporalio/samples-go/worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: temporal-worker-config
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
